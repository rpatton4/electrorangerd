// Package config is a secondary adapter that implements core.ConfigStore. It
// manages user preferences, connection profiles, and the recent-files list by
// persisting them as a JSON file on the local filesystem.
package config

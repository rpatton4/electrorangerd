// Package panel hosts the side panels that surround the active mode view —
// most importantly the peek panel, which shows a slice of one mode (typically
// the dictionary entry for the currently-selected schema element) while the
// user remains in another mode.
package panel

// Package domain contains the pure entity and value types that form the core
// of the electrorangerd data model. It imports nothing from within this module
// and mutates nothing — services that operate on these types produce new values
// rather than modifying existing ones in place.
package domain
